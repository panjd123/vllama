# Vllama Server Configuration
# Copy this file to .env and modify as needed

# Vllama server host and port
VLLAMA_HOST=0.0.0.0
VLLAMA_PORT=33258

# vLLM instance port range
VLLAMA_VLLM_PORT_START=33300
VLLAMA_VLLM_PORT_END=34300

# Auto-unload configuration
# Timeout in seconds before unloading idle models
VLLAMA_UNLOAD_TIMEOUT=1800

# Unload mode: 1 (light sleep), 2 (deep sleep), 3 (full stop)
VLLAMA_UNLOAD_MODE=2

# Hugging Face home directory (replaces deprecated TRANSFORMERS_CACHE)
HF_HOME=/root/.cache/huggingface

# Hugging Face token for private models (optional)
# HF_TOKEN=hf_xxxxxxxxxxxxx
